# 🚀 راهنمای سریع راه‌اندازی

این راهنما به شما کمک می‌کند تا در کمتر از 5 دقیقه ربات را راه‌اندازی کنید.

## ✅ پیش‌نیازها

- Node.js نسخه 14 یا بالاتر ([دانلود Node.js](https://nodejs.org/))
- یک حساب تلگرام

## 📝 مراحل راه‌اندازی

### مرحله 1: دریافت توکن ربات (2 دقیقه)

1. در تلگرام به [@BotFather](https://t.me/BotFather) پیام دهید
2. دستور `/newbot` را بفرستید
3. یک نام برای ربات انتخاب کنید (مثلاً: "Music Search Bot")
4. یک username برای ربات انتخاب کنید (باید به `bot` ختم شود، مثلاً: `my_music_search_bot`)
5. توکن ربات را کپی کنید (شبیه: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### مرحله 2: تنظیم پروژه (1 دقیقه)

```bash
# 1. کلون کردن پروژه
git clone https://github.com/shehneh/tanziir-join-miniapp.git
cd tanziir-join-miniapp

# 2. نصب وابستگی‌ها
npm install

# 3. ایجاد فایل .env
cp .env.example .env
```

### مرحله 3: تنظیم فایل .env (30 ثانیه)

فایل `.env` را باز کنید و اطلاعات خود را وارد کنید:

```env
# توکن ربات که از BotFather دریافت کردید
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz

# آدرس کانال موسیقی شما (اختیاری - برای جستجو در کانال)
MUSIC_CHANNEL=@your_channel_username

# شناسه کاربری ادمین (اختیاری - برای دریافت گزارش خطا)
ADMIN_USER_ID=your_telegram_user_id
```

### مرحله 4: اجرای ربات (10 ثانیه)

```bash
# تست تنظیمات
npm test

# اجرای ربات
npm start
```

اگر همه چیز درست باشد، باید این پیام را ببینید:
```
🤖 Bot started successfully!
📢 Monitoring channel: @tanziir
```

## 🎉 استفاده از ربات

1. در تلگرام ربات خود را پیدا کنید (با username ربات)
2. دستور `/start` را بفرستید
3. نام آهنگی را که می‌خواهید بنویسید
4. ربات آهنگ را برای شما جستجو و ارسال می‌کند!

### مثال‌های جستجو:

```
عماد طالب زاده
```

```
دلم گرفته
```

```
حمید عسکری
```

## ⚙️ تنظیمات پیشرفته (اختیاری)

### جستجو در کانال

برای فعال کردن جستجوی کانال:

1. یک کانال عمومی در تلگرام بسازید
2. ربات را به عنوان ادمین به کانال اضافه کنید
3. آدرس کانال را در `.env` وارد کنید
4. مستندات کامل در فایل `CHANNEL_SEARCH.md`

### اجرای ربات در سرور (Production)

برای اجرای دائمی ربات از PM2 استفاده کنید:

```bash
# نصب PM2
npm install -g pm2

# اجرای ربات با PM2
pm2 start bot.js --name music-bot

# مشاهده وضعیت
pm2 status

# مشاهده لاگ‌ها
pm2 logs music-bot

# توقف ربات
pm2 stop music-bot

# راه‌اندازی خودکار هنگام بوت سیستم
pm2 startup
pm2 save
```

## 🐛 رفع مشکلات رایج

### ربات شروع نمی‌شود

```bash
# بررسی نسخه Node.js
node --version  # باید 14 یا بالاتر باشد

# بررسی نصب وابستگی‌ها
npm install

# بررسی فایل .env
cat .env  # مطمئن شوید توکن درست است
```

### ربات پاسخ نمی‌دهد

1. مطمئن شوید ربات روشن است (`npm start`)
2. توکن ربات را در `.env` بررسی کنید
3. اتصال اینترنت سرور را چک کنید
4. لاگ‌های ربات را بررسی کنید

### جستجو نتیجه نمی‌دهد

1. مطمئن شوید اتصال اینترنت دارید
2. سرویس iTunes Search ممکن است در برخی کشورها فیلتر باشد
3. از VPN استفاده کنید

## 📚 مستندات بیشتر

- [README.md](README.md) - مستندات کامل فارسی
- [CHANNEL_SEARCH.md](CHANNEL_SEARCH.md) - راهنمای جستجوی کانال
- [GitHub Issues](https://github.com/shehneh/tanziir-join-miniapp/issues) - گزارش مشکل

## 💬 پشتیبانی

سوال دارید؟ 
- Issue در GitHub ایجاد کنید
- در Discussions شرکت کنید

---

✨ موفق باشید!
